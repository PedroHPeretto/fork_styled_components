import { FormContent, FormHero, HeroTitle } from './Hero'

const HeroComponent = () => (
  <FormHero>
    <FormContent className="container">
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </FormContent>
  </FormHero>
)

export default HeroComponent
